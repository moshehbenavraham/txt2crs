# Backend Packages

This directory contains Python distributions that are independently testable
and buildable but can also be consumed by the backend application.

- [`txt2crs`](txt2crs/) — the reusable course-generation engine.

Each package owns its source tree, package metadata, tests, license, and
package-specific documentation.
